{
  "name": "anor-eimzo",
  "version": "1.1.2",
  "description": "Eimzo plugin for npm package",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "tsc && COPY package.json dist",
    "pub": "tsc && cd dist && npm publish"
  },
  "keywords": [
    "anor-eimzo"
  ],
  "author": "Akhrorbek Kuchkarov",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.8.4",
    "@types/node": "^16.11.68"
  }
}
